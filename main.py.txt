from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def read_root():
    return {"message": "SondMusic API funcionando!"}

@app.post("/generate")
def generate_music(prompt: str, length: int):
    # Aqui você colocaria o código que gera a música
    return {"prompt": prompt, "length": length, "status": "gerado"}
